# Telegram Bot для Research API

Telegram бот для интеллектуального поиска информации.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd backend-go
go get github.com/go-telegram-bot-api/telegram-bot-api/v5
```

### 2. Создание бота в Telegram

1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям и получите токен
4. Сохраните токен

### 3. Настройка переменных окружения

Добавьте в `.env`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

### 4. Запуск бота

```bash
# Локально
export TELEGRAM_BOT_TOKEN="your_token"
go run ./cmd/tgbot/main.go

# Или собрать и запустить
go build -o bin/tgbot ./cmd/tgbot/main.go
./bin/tgbot
```

## 📋 Команды бота

- `/start` - Приветствие и инструкция
- `/mode` - Выбрать режим работы (Auto/Simple/Pro)
- `/newsession` - Начать новую сессию (очистить контекст)
- `/help` - Показать справку

## 🎯 Режимы работы

1. **Auto** - Бот автоматически выбирает режим
2. **Simple** - Быстрый поиск для простых вопросов
3. **Pro** - Глубокий анализ с контекстом беседы

## 🔧 Конфигурация

Переменные окружения:

```env
TELEGRAM_BOT_TOKEN=required     # Токен бота от BotFather
API_URL=http://localhost:8000   # URL вашего API (по умолчанию localhost:8000)
```

## 📝 Примеры использования

1. Простой вопрос:

   ```
   Кто изобрел телефон?
   ```

2. Сложный вопрос с контекстом:

   ```
   Сравни экономические политики США и ЕС
   Какие были последствия?
   А в долгосрочной перспективе?
   ```

3. Смена режима:
   ```
   /mode → выбрать Pro
   Проанализируй влияние изменения климата на сельское хозяйство
   ```
